export const Item = 
    (item) => { 
        return { 
        content: item.CONTENT,
        contentImage: item.CONTENT_IMG,
        links: item.LINKS,
        right: item.RIGHT,
        title: item.TITLE, 
        wrong: item.WRONG,
        usage: item.USAGE
     } }
